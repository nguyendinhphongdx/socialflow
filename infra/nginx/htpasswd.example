# Basic auth file template for /grafana/ access.
# Generate with: htpasswd -B -c htpasswd opsadmin
# Copy as ./infra/nginx/htpasswd (gitignored).
# Format: <user>:<bcrypt-hash>
# Example (do NOT use this hash in production):
#   ops:$2y$05$RandomHashGoesHereReplaceMe
